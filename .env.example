# PostgreSQL Database Configuration
# These environment variables are used by drizzle-kit for migrations
# The application itself uses config.json (managed by ConfigManager)

DB_HOST=localhost
DB_PORT=5432
DB_NAME=turbo_julius
DB_USER=postgres
DB_PASSWORD=password123
